HTTP Crash Course # Brad Traversy

00:24:00	Simulating on Postman a POST Request (Form Submit to send Data) x-www-form-urlencoded
00:29:00	Pour eviter "Header already sent error" make sure to put the return statement under the if clause.
00:33:00	req.params will access the url values; req.body will access the form data (or the json data).

